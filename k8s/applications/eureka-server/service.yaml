apiVersion: v1
kind: Service
metadata:
  name: eureka-server
  labels:
    env: development
    tier: backend
    app: eureka
spec:
  ports:
    - port: 8761
      targetPort: 8761
  selector:
    env: development
    tier: backend
    app: eureka
